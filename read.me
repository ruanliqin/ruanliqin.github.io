已将 index.html 拆分为三个文件：
styles.css — 包含所有 CSS 样式
script.js — 包含所有 JavaScript 代码
index.html — 已更新，移除了内联的 <style> 和 <script> 标签，改为引用外部文件
现在文件结构更清晰，便于维护。功能保持不变，网站仍可正常工作。检查是否有错误：
现在项目包含：
index.html — HTML 结构（已精简）
styles.css — 所有样式
script.js — 所有 JavaScript 功能